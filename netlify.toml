# Configurazione deploy Netlify — sito statico, nessuna build
# Test deploy continuo da repo privato: 2026-06-27
[build]
  publish = "."
  command = ""

# Serve il gestionale direttamente sulla root (rewrite, URL pulito)
[[redirects]]
  from = "/"
  to = "/gestionale-oculista.html"
  status = 200
